if(WITH_NATIVE_APP)
    add_subdirectory (NativeWindow)
endif()
if (WITH_NATIVE_ACTIVITY)
    add_subdirectory (native-activity)
endif()
if(WITH_NATIVE_CLIENT)
    add_subdirectory (NativeClient)
endif()

